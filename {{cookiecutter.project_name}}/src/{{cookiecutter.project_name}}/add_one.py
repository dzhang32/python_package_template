def add_one(x: int) -> int:
    """
    Example function that adds one to the input.

    Args:
        x: The input number.

    Returns:
        The input number plus one.
    """

    return x + 1
